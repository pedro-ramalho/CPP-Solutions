/*
int rollDie(int low = 1, int high = 6)
	{
	assert(high >= low);
	return (rand() % (high - low + 1)) + low;
}
*/

//This function generates a random integer between 1 and 6 (inclusive)

//If the expression inside the assert is false, then an error occurs.
